import styled from 'styled-components';
import {
  space,
  layout,
  flexbox,
  grid,
  position,
  typography,
  color,
  border,
  shadow,
} from 'styled-system';

export const Box = styled('div')(
  space,
  layout,
  flexbox,
  grid,
  position,
  typography,
  color,
  border,
  shadow
);
